{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 execute securevote --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute securevote --remote --file=./migrations/0001_init.sql"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.0",
    "@noble/hashes": "^1.5.0",
    "ethers": "^6.17.0",
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20241200.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}